package com.mememan.vfxlib.vfx.effects.base;

import net.minecraft.world.level.Level;

/**
 * An enum representing the existence of an effect, either in graphical (on-screen, GUI) or physical (in a {@link Level}).
 */
public enum EffectPresence {
    GRAPHICAL,
    PHYSICAL
}
